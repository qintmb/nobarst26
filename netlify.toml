[build]
  functions = "netlify/functions"
  publish = "."

# Redirect /api/* to the Netlify function (preserves path)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/submit"
  status = 200
  force = true
